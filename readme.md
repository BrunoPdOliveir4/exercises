## Task 1
Exercise 1 -
> It was created one file for each of kinds of function asked.

It was created random cases using each of the function types 

Exercise 2 - 
> It is just necessary to run html file and it will auto-run the code.

It is a script with a list class to create, remove and render itself in html
It also have a create button function that receive a value and a function to add to it
Then It is created a input and it value is added with add button or you can also remove the last item with remove button
